export interface RegistrationDetails {
    email: string;
    password: string;
    matchingPassword: string;
    firstName: string;
    lastName: string;
}

export class CredentialDetails{
    username:string;
    password:string;
}